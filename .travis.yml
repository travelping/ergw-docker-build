sudo: required

services:
  - docker

script:
  - docker build -t grundrausch3n/ergw-docker-build .

after_success:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
    docker push grundrausch3n/ergw-docker-build
